{
  "name": "legonode-framework",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd examples/basic-api dev",
    "build": "bun run --cwd packages/legonode build",
    "test": "bun test",
    "lint": "echo \"lint: not configured yet\""
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
