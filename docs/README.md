# Docs

- **[ARCHITECTURE.md](./ARCHITECTURE.md)** — Request lifecycle, routing (`router/`), middleware, plugins, dev mode.
- **[PERFORMANCE.md](./PERFORMANCE.md)** — Throughput and benchmarking notes.
- **[todo.md](./todo.md)** — Internal checklist.

**Layout reminder:** default `appDir` is `./src`. HTTP routes live in **`src/router/**`**; events in **`src/events/`**, cron in **`src/cron/`**.
